# Environment variables for ClawDeck Docker Compose

# Copy this file to .env and update with your values

# Application
NODE_ENV=development
PORT=3000
API_TIMEOUT_MS=300000
LOG_LEVEL=info

# Database
POSTGRES_PASSWORD=clawdeck_dev_password_change_me
POSTGRES_DB=clawdeck_development
POSTGRES_PORT=5432

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET=change-this-in-production-use-openssl-rand-hex-32

# Ports (if defaults conflict)
API_PORT=3000
POSTGRES_PORT=5432
REDIS_PORT=6379
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
